Add fullscreen mode to the Pygame example

Modified the Pygame example to run in fullscreen mode by utilizing the `pygame.FULLSCREEN` flag and getting the screen dimensions using `pygame.display.Info()`. This change allows the example to run on the detected screen resolution in fullscreen mode.
